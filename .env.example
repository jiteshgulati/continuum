PORT=3001
HOST=127.0.0.1
APP_ORIGIN=http://localhost:5173
DATABASE_PATH=./data/continuum.sqlite
UPLOAD_DIR=./data/uploads
AI_PROVIDER=compatible
AI_BASE_URL=https://api.openai.com/v1
AI_API_KEY=
AI_MODEL=
# Set true only behind HTTPS in production.
COOKIE_SECURE=false
FILE_STORAGE=local
# For private AWS object storage:
# FILE_STORAGE=s3
# S3_BUCKET=your-private-bucket
# No sample accounts are created automatically in production.
# ENABLE_DEMO=false
# Optional Bedrock provider; uses the standard AWS credential chain.
# AI_PROVIDER=bedrock
# AWS_REGION=us-east-1
# BEDROCK_MODEL_ID=
